info:
  name: login copy
  type: http
  seq: 4

http:
  method: POST
  url: https://localhost:7172/api/auth/register
  body:
    type: json
    data: |-
      {
        "nic": "200012345679",
        "name": "Test Prosumer 2",
        "email": "prosumer2@example.com",
        "password": "TestPassword123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
