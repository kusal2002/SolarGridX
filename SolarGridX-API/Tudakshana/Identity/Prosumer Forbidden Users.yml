info:
  name: Test Prosumer Cannot Manage Users
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:5084/api/auth/users
  auth:
    type: bearer
    token: "{{prosumerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
