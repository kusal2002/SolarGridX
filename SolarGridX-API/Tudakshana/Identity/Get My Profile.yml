info:
  name: Get My Profile
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5084/api/auth/me
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
