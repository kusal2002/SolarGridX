info:
  name: Request Prosumer Deactivation
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5084/api/auth/users/{{prosumerNic}}/deactivation-request
  auth:
    type: bearer
    token: "{{prosumerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
