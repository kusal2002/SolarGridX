info:
  name: Test Login - Prosumer JWT
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:5084/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "{{prosumerEmail}}",
        "password": "{{prosumerPassword}}"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
