info:
  name: Test Revoked JWT Is Rejected
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:5084/api/auth/me
  auth:
    type: bearer
    token: "{{prosumerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
