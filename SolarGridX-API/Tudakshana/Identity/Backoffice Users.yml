info:
  name: Backoffice List Users
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:5084/api/auth/users?role=Prosumer
  auth:
    type: bearer
    token: "{{backofficeToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
